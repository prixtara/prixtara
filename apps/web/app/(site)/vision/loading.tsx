/**
 * Vision page loading placeholder shell.
 *
 * Architectural freeze: Semantic placeholder shell only — no visual styling or animations.
 */
export default function VisionLoading() {
  return (
    <main aria-busy="true" aria-live="polite">
      <h1>Our Vision</h1>
      <p>Loading vision and architectural principles...</p>
    </main>
  );
}
